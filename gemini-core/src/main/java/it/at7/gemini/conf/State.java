package it.at7.gemini.conf;

public enum State implements Comparable<State> {
    STARTING,
    STARTED,
    GEMINI_MODULES_LOADED,
    SCHEMA_STORAGE_INITIALIZED,
    SCHEMA_EVENTS_LOADED,
    FRAMEWORK_SCHEMA_RECORDS_INITIALIZED,
    PROVIDED_CLASSPATH_RECORDS_HANDLED,
    INITIALIZED,
    API_INITIALIZATION,
    API_INITIALIZED,
    GUI_INITIALIZATION,
    GUI_INITIALIZED
}
